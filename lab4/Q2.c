#include <stdio.h>
#include <stdlib.h>

int main()
{
    char name[] = {'R', 'o', 'b', 'e', 'r', 't'};

    puts( name );

    scanf( "%s", "Philip");

    strcpy( name, "Philip" );

    *(name + 5) = "a";

    name = "Philip";

    getchar();
    return 0:

}
